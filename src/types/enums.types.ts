export enum Role {
  SuperAdmin = "SuperAdmin",
  Admin = "Admin",
  Doctor = "Doctor",
  User = "User",
  Patient = "Patient",
}

export enum HospitalType {
  Hospital = "Hospital",
  Clinic = "Clinic",
}

export enum AppointmentStatus {
  Pending = "Pending",
  Approved = "Approved",
  Completed = "Completed",
  Canceled = "Canceled",
}
