export enum Role {
  SuperAdmin = "SuperAdmin",
  Admin = "Admin",
  Doctor = "Doctor",
  User = "User",
  Patient = "Patient",
}

export enum HospitalType {
  Hospital = "Hospital",
  Clinic = "Clinic",
}
